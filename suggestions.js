// const handleButtonClick = (event) => {
//     const calcDisplay = document.getElementById("display-box").innerHTML;
//     if (event.target.value is not a number and calcDisplay.length === 0) {
//         alert(no)
//         return
//     }
//     const last = calcDisplay[calcDisplay.length - 1];
//     if (last char not number and event.target.value not number) {
//         alert(no)
//         return
//     }
//     // Calling updateDisplayBox function
//     updateDisplayBox(event.target.value);
// }; 

// // "12+12+12"
//     // "12+12+12" => [12, +, 12] "+12"
//     // "24+12" => [24, +, 12]
//     // "36"
//     const replaceHandler = (left, op, right) => {
//         // find out what op is and use it with left and right
//     }
//     let currentExpr = splitCalc;
//     const regex = /([0-9]+\.)([+-*/])([0-9]+\.)/;
//     while (regex.test(splitCalc)) {
//         currentExpr = currentExpr.replace(regex, replaceHandler)
//     }


//     // Defining a function to be called when user clicks an input button
// const handleButtonClick = (event) => {
//     const calcDisplay = document.getElementById("display-box").innerHTML;
//     if (event.target.value is not a number and calcDisplay.length === 0) {
//         alert(no)
//         return
//     }
//     const last = calcDisplay[calcDisplay.length - 1];
//     if (last char not number and event.target.value not number) {
//         alert(no)
//         return
//     }
//     // Calling updateDisplayBox function
//     updateDisplayBox(event.target.value);
// };